package memory.flash.ref.val;

import system.NMSystem;

public class KeywordValidation extends ReferenceCondition {
    @Override
    public boolean check(String potentialName) {
        return true;
    }
}
